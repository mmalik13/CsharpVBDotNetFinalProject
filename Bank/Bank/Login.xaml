<Window x:Class="Bank.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bank"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <TextBlock Text="ID" Grid.Row="0"/>
        <TextBox x:Name="txtBoxID" Grid.Row="1"/>

        <TextBlock Text="Password" Grid.Row="2"/>
        <TextBox x:Name="txtBoxPassword" Grid.Row="3"/>
        <Button x:Name="btnLoginWindow" Grid.Row="4" Content="Login" Click="BtnLoginWindow_Click"/>
    </Grid>
</Window>
